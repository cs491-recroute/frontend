export enum OPERATIONS {
  eq = 'eq',
  ne = 'ne',
  gt = 'gt',
  lt = 'lt',
  gte = 'gte',
  lte = 'lte',
  includes = 'includes'
}

export enum STAGE_TYPE {
  FORM = 'FORM',
  TEST = 'TEST',
  INTERVIEW = 'INTERVIEW'
}